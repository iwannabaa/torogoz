'use strict';

/**
 * @ngdoc function
 * @name torogozApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the torogozApp
 */
angular.module('torogozApp')
  .controller('AboutCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
